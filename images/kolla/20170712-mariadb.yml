---
# project: mariadb
#   build: 

mariadb_revision: 4

mariadb_version: "10.0"

mariadb_image: "betacloud/mariadb"
mariadb_tag: "{{ mariadb_version }}-{{ mariadb_revision }}"
---
# project: mariadb
#   build: 20170712

mariadb_revision: 4

mariadb_version: "10.0"

mariadb_image: "betacloud/mariadb"
mariadb_tag: "{{ mariadb_version }}-{{ mariadb_revision }}"
---
# project: mariadb
#   build: 20170712

mariadb_revision: 4

mariadb_version: "10.0"

mariadb_image: "betacloud/mariadb"
mariadb_tag: "{{ mariadb_version }}-{{ mariadb_revision }}"
